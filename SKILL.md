---
name: audio-transcribe
description: 音频/视频转文字执行指南：优先复用当前电脑已经可运行的转写能力并立即派子代理开工，中文 ASR 榜单、免费额度和更优路线并行查询；覆盖说话人、时间戳、多路 ASR 与受约束校对。用于转写录音、播客、会议或视频，以及核验已有自动稿；仅总结或改写现成文字时不触发。
---

# 音频转文字（audio-transcribe）

本 skill 的目标是尽快交付可靠文字稿。当前电脑有能完成主要交付的管线时立即开工；市场比较不挡在转写前面。

## 边界

负责寻找现成文字稿、音频转写和必要校对，不负责观点改写或内容再创作。上传第三方云端前需要用户同意；收费调用需要用户授权。

## 1. 先开工

1. 先看用户已经提供的文件、字幕或会议纪要；明显可能有官方文字稿的公开节目，可与转写并行查找，不等待联网结果才开始。
2. 若本 skill 目录已有 `local-config.md`，只读取与本任务相关的现成本地模型、脚本和已可用端点；没有该文件就做一次轻量探测，不创建台账，也不因此停顿。
3. 从请求和素材判断语言、时长，以及是否需要说话人、时间戳或字幕格式。用户没有指定时，默认交付可读纯文本。
4. 只要现成管线能满足主要交付，立即派转写子代理执行。任务较长时按文件或时间段拆成互不重叠的块；主代理继续处理交付结构和后续合并。
5. 有空闲并发槽时，再派一个只读子代理按 [`references/asr-benchmarks.md`](references/asr-benchmarks.md) 查与素材匹配的中文评测结果和当前官方免费额度。研究只提出是否值得切换或增加第二路线，不阻塞已开始的转写。

本机没有可执行路线，或者下一步需要登录、授权上传、下载大型模型或产生新费用时，再向用户提出一个简短选择。

## 2. 实时选型

执行优先使用当前可运行且不新增费用的可靠管线；推荐候选时优先当前有免费额度的高质量方案。用户明确追求最高质量时，再按素材场景选择当前中文评测表现更好的方案。

比较时只看会改变选择的几项：

- 与素材匹配的普通话、会议、方言或专业领域 CER；
- 说话人、时间戳、热词和字幕导出；
- 本机是否能直接执行；
- 当前免费额度、有效期和超额价格。

GigaSpeechBench 可用于中文方言和垂直领域准确率比较；WenetSpeech、SpeechColab Leaderboard 等是稳定评测集或复现入口。WER/CER 排名不代表端到端延迟或流式稳定性，实时字幕任务还要看产品当期的流式能力。

不维护静态模型排名、价格或额度表。榜单与官方页面只在任务发生时读取。

## 3. 转写执行

基础稿跑一条现成管线；高质量或可发布稿在不阻塞首条管线的前提下并行第二条实质独立的 ASR，用两版分歧定位回听重点。

- 超长音频约两小时以上时在静音处切段，段间保留约 60–120 秒重叠。
- 热词只放高频且已知容易错的专名，避免偏置污染无关片段。
- 两路输出天然分段不同，先按时间和句边界对齐；一致部分默认保留，重点处理分歧。
- 时间戳单位先确认，避免把秒和毫秒混用。

需要多路合并、说话人对齐或 JSON 交付时，统一为：

```json
{"start_ms": 0, "end_ms": 1000, "speaker": null, "text": "...", "source": "engine", "flag": null}
```

普通纯文字稿直接使用引擎输出，不做额外格式转换。

## 4. 校对

校对只修识别错误，不改说话人的观点、口语风格或句子结构。听不清处标注，不猜写。

- 同音字、错别字、专名、术语和标点可以结合上下文直接修正。
- 人名、机构、数字、引语和否定词优先用原音频或可检索来源核实。
- 长稿分片校对，完成后统一专名异写。
- 带说话人或字幕结构时检查标签和时间顺序；普通文字稿只确认内容完整可读。

## 5. 交付

先交付文字稿，再简短说明本次实际使用的管线。随后根据本次实时查询结果补充：

- **有免费额度**：当前有免费额度、且适合本素材的可靠方案；
- **完全免费**：可在本地运行、不产生 API 调用费的方案；
- **高质量**：与本素材场景匹配的当前优选方案。

同一个方案同时命中多类时合并，不为凑三项重复推荐，也不展开成市场报告。

## 踩坑速查

- 引擎可能把音色相近的两人合并，人数异常时抽听核对。
- SenseVoice 等长音频路线需要合适的 VAD；不分段可能明显退化。
- 固定时间窗在两路密度差异大时容易对错句，使用滑动搜索、句边界和单调约束。
- 同名目录可能只有一份真正含音频，按文件内容选择，不取第一个路径匹配。
