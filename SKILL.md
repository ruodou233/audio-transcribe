---
name: audio-transcribe
description: 音频/视频转文字全流程：先找现成文字稿，找不到再按质量要求与产出形态选路径——便宜线（免费单引擎快速稿）/标准线（说话人分离结构化稿）/优质线（双ASR交叉验证+分歧LLM裁决精确稿）。当用户要"把音频/播客/录音/视频转成文字稿/逐字稿"，或问"哪种转写方案便宜/准确"时触发。
---

# 音频转文字（audio-transcribe）

> 定位：本 skill 是给 Agent 的**决策与执行指南**——从"要文字稿"到"拿到可靠文字稿"的完整流程。所需脚本（格式转换、对齐、校验）由执行 Agent 按本文的数据格式与校验规则现场生成，不随包提供固定实现。
> 理由前置：转写是"选型决定成本、流程决定质量"的任务——先选对方案再动手，比转完再补救便宜一个数量级。

## 边界

本 skill 负责文字稿获取、音频转写与最小必要校正。不负责：观点润色、摘要改写、内容再创作、深度事实核查（仅在用户要求或人名/机构名高风险时联网核查）。
**隐私门槛**：上传第三方云端 ASR 前，先提醒用户确认音频内容可以外传；不可外传的走本地方案。
**版权与访问边界**：不绕过登录、付费或访问控制获取文字稿；第三方全文仅在授权允许时复用，否则提供来源链接，或仅用于核对用户有权处理的音频。

## 两条全程不变量（后文只引用，不再重复展开）

**证据优先级**（修正时按此排序取证）：
`原始音频回听 ≈ 联网检索核实 > 多路 ASR 一致 > 上下文推断 > 保留原样`
LLM 校对的任务**就是凭上下文修正 ASR 的识别错误**（同音字、错别字、专名、术语）——这正是用文字模型再审一遍的目的，该改就改。但高风险字段（人名、机构、数字、引语、否定词）的修正尽量用检索或回听佐证；佐证不了又拿不准的标记待核，不静默定夺。**两版 ASR 一致只是"默认保留"，不豁免高风险字段抽检——两个引擎可能犯同样的错。**

**修正的边界**：只修"识别错误"，不动"表达本身"——观点性措辞、口语风格、口误照录不润色；听不清的用 [标注] 而非编内容补全；不增删信息、不改写句子结构；昵称/简称保留原文，按「称呼（全名；未确证时注明）」加注。所有修改 diff 留痕，可追溯。

## 第零步：判断有没有现成文字稿，有才去找

先花十秒判断该作品**是否可能存在现成稿**：官方节目、知名播客/访谈、机构演讲、头部自媒体这类大概率有——值得搜一轮；普通视频、个人录音、会议/采访原始素材、小众内容大概率没有——不浪费时间，直接进转写。判断"可能有"时按以下来源找（成本从低到高）；用户明确要求重新转写或需要逐字级证据时跳过本步：

1. **手头已有**：用户提供的文件、本地目录、协作平台文档/会议纪要。
2. **作者本人渠道**：博客、公众号（访谈节目常有官方精校版）、知乎/Substack。
3. **媒体实录**：科技媒体常发布圆桌/对谈全场实录，多家可交叉核对。
4. **平台字幕接口**：视频平台官方/CC 字幕（可用率高）；拿到后仍需抽检——自动字幕错误多、缺说话人。
5. **都没有**：下载音频进转写流程。

**可信度分级**：官方精校稿 > 平台官方字幕 > 媒体整理稿/自动字幕 > 第三方转载。使用前记录：来源、逐字稿/整理稿/摘要、有无时间戳。媒体"实录"未必逐字——引用前先确认。

## 第一步：前置画像（决定方案）

先确认五件事：**语言**（中/英/混合）、**说话人数**（不预设！见踩坑速查）、**时长**、**预算/质量要求**、**音频可否上云**。

**按质量要求与产出形态定路径**（三选一，后文流程按路径裁剪；路径只定需求，引擎映射集中在第二步）：
- **便宜线（快速稿）**：自用、抓要点、能容忍个别错字，不要求说话人标注/结构化导出——单引擎一条线，可选一遍轻校对（遵守"修正的边界"）；双 ASR、diff 裁决、回听抽检全部跳过。
- **标准线（结构化稿）**：需要说话人分离、时间戳/JSON 导出或热词控制，但不到出版级——带分离能力的引擎单线 + 一遍受约束校对 + 高风险字段（人名/机构/数字/引语/否定词）抽检；此类错误密集时升级优质线。
- **优质线（精确稿）**：逐字引用、公开发布、对人名/数字敏感——双引擎交叉验证全流程（第三步）+ 完整校对与可追溯验收（第四步）。

三点通用约束：①便宜线/标准线产出标注"单引擎未交叉验证"；②说话人分离给出的是匿名标签，落到真名靠称呼线索/名单/用户确认归正（见第三步"说话人归正"），无法确证的保留标签；③音频不可上云 → 在所选路径内换本地引擎（分离需求可用本地 FunASR 生态），验真流程不降级。

## 第二步：方案选型（按价格从低到高）

> 价格与额度为作者 2026-07 核验，官方计价以各平台当期文档为准（阿里云百炼控制台、platform.openai.com/docs）。执行前核验当前账户的实际计价与剩余额度，不得把任何一线假定为免费。CER 数字为社区基准，只作初筛，复用前用当前任务样本重测。

| 方案 | 官方计价 | 每小时估算 | 适用 | 短板 |
|---|---|---|---|---|
| 飞书妙记 | 免费（截至 2026-07，飞书个人版实测） | ¥0 | 多人访谈/播客；在线对照、可搜索 | 说话人为通用标签；导出/API 弱 |
| 本地 SenseVoice-Small | 免费 | ¥0 | 单人批量（视频文案/字幕补救），社区基准中文 CER≈8% | **必须配 VAD 切分**（直接喂长音频会解码退化成乱码）；专名/人名反复出错 |
| 本地 faster-whisper | 免费 | ¥0 | 兼容回退基线 | 社区基准中文 CER≈20%（模型规格越小越差），人名/网络用语易错 |
| 本地 FunASR 生态（含分离） | 免费 | ¥0 | 音频不可上云但需说话人分离的场景 | 本地算力与安装成本；作者未实测，用前小样验证 |
| 阿里百炼 fun-asr | 0.00022 元/秒 | ≈¥0.79/小时 | 多说话人首选：说话人分离+热词表；长音频异步 | 需上云；启用分离时按 ≤2h 切片 |
| gpt-4o-transcribe | $0.006/min（mini 版 $0.003/min） | ≈$0.36/小时 | 重点样本高精度返工 | 最贵；无说话人分离 |
| **双 ASR 交叉验证管线**（下节） | 一条免费线+一条低价线 | ≈¥0.79/小时起 | 高要求多人录音（访谈/播客逐字稿） | 流程最重 |

免费额度单列（不计入标准价，随平台政策变动）：fun-asr 新用户常有约 10 小时级免费额度（2026-07 核验）。

路径映射（引擎映射唯一维护处；默认按中文语料实测，英文/混合语料先用短样本试跑再定引擎）：
- 便宜线：多人 → 免费会议纪要工具（如飞书妙记）；单人 → 本地 SenseVoice。
- 标准线：fun-asr 单线（分离+热词）；不可上云时用本地 FunASR 生态替代。
- 优质线：两条厂商差异大的引擎交叉——多人默认 fun-asr + 妙记；单人可关闭分离，选两个差异足够大的引擎（如 fun-asr + gpt-4o-transcribe）。
- gpt-4o-transcribe 默认只作个别关键片段的高精度返工；短素材整段皆关键时，按预算可全程使用。

## 统一数据格式（Agent 现场生成脚本时遵守）

所有引擎的输出先转换为统一段格式再进入后续流程，每段：

```
{ "start_ms": int, "end_ms": int, "speaker": str|null, "text": str, "source": "engine名", "flag": null|"disputed"|"uncertain" }
```

校验脚本的固定断言：时间戳单调不回退、相邻段无大空洞/重叠（阈值按素材定）、speaker 标签集合稳定、`disputed` 段占比可计算。**"行数守恒"只用于同一份稿件进出 LLM 校对的前后比对**，不适用于两份天然分段不同的 ASR 原稿之间。

便宜线快速稿若不进入合并、裁决或自动化管线，可直接交付引擎原生导出，免去格式转换。

## 第三步：转写执行（标准线复用其中单线部分；双 ASR 交叉为优质线）

各路径执行范围：便宜线不进本节（单引擎直转）；标准线执行第 1 条的 A 线预处理/上传、第 2 条热词克制与第 5 条说话人归正，跳过双线合并与 diff 裁决；优质线全部执行。第 2 条热词克制适用于所有用到 fun-asr 的路径。

1. **双 ASR 并行**（关键：两条引擎厂商/模型差异要够大）：
   - A 线：fun-asr（diarization_enabled=true，**不设 speaker_count**；用官方临时存储上传，不为 ASR 暴露本机公网入口）
   - B 线：飞书妙记（免费）
   - 任一线不可用 → 降级为单 ASR + 疑点标注 + 抽样复听，并明确告知用户降级。
   - 预处理：转 16kHz 单声道 WAV（macOS 自带 afconvert 即可，无需 ffmpeg）；超长音频（约 2h 以上）在静音处切段，段间重叠默认约 90 秒（60–120 秒内按静音点调整；该默认值为作者实测经验值，见文末案例注记）。
2. **热词表克制**：只放高频且已知必错词；低频专名默认不放（教训：热词会劫持无关音段，修 10 处会新造 3 处更隐蔽的错）。
3. **合并与脚本校验**：重叠区投票对齐 speaker_id；按上节统一格式做脚本校验（不用 LLM 复查结构）。
4. **diff 裁决**（省 token 关键）：两版按时间窗对齐，一致部分默认采信（高风险字段仍抽检）；仅分歧片段带上下文送 LLM，按"证据优先级"取证：**能通过联网检索确定的（人名写法、作品名、机构、术语）用带搜索能力的强模型直接查实**；检索定不了且高风险的回听原音频；普通低风险差异由 LLM 凭上下文在两版读法中选合理的。禁止 LLM 重写全局时间戳；音乐/听不清/多人同说用 [标注] 而非补写。普通裁决用中文实测好且便宜的档位即可，需要检索核实的换带联网的强模型。
5. **说话人归正**：问句比例等启发式只做初筛，结合称呼线索、上下文角色判断；分歧句才送 LLM；多主持场景在文件头注明标签口径。
6. **失败判据**：两版时间轴严重偏移、分歧占比异常（经验阈值：`disputed` 段总时长占比 >15%，来源见案例注记）、speaker 分布明显失真时，中止自动合并，回头修 ASR 参数或切段，不硬裁。

## 第四步：校对（按路径分档）

实测有效组合是「**较强 ASR + LLM 逐行校对**」，不要选弱 ASR 靠大模型改写补救。分档：便宜线可选一遍轻校对（只走下方校对规则①—③）；标准线一遍受约束校对 + 高风险字段抽检；优质线完整校对 + 全部可追溯验收。

校对规则：①保留行号/时间戳/说话人标签完全不变；②**凭上下文和热词表大胆修正识别错误**——错别字、同音字、专名、术语、标点，这是校对的目的；③修正遵守"修正的边界"不变量（不动表达本身、不补听不清、全量 diff 留痕），拿不准的标记待核；④长稿切 chunk 分片校对，逐片落盘可断点续跑。

可追溯验收：raw 原稿保留不覆盖；校对后 diff 出修改清单；关键人名/数字/引用句/低置信片段按"证据优先级"回听抽检；批量任务把验收做成固定脚本（行号连续、时间戳单调、三元结构保留、speaker 分布前后一致、低相似窗口抽样复核）。

## 踩坑速查（实战教训）

- 说话人数不能预设：有引擎会把音色相近的两人合并成同一 speaker_id——发现说话人数少于预期时人工抽听核对。
- 长音频时间轴单位误判：>1000 秒的内容被当成毫秒再除一次，对齐全错——对齐前先断言单位。
- 固定 60 秒窗口对齐在两路密度悬殊时会框错内容（假阳性）——用滑动搜索+句边界合并+单调约束。
- SenseVoice 不配 VAD 直接喂长音频 → 解码退化成乱码。
- 批量实验选样本要按时长过滤（超长素材会走单引擎降级路径，产不出对比数据）。
- 同一素材可能存在多个同名目录但音频只在其中一份——按"真正含音频文件"选目录，不取第一个匹配。

## 案例注记（作者特定样本实测，非通用基准）

- 3 小时双人中文访谈播客（单样本，勿外推）：fun-asr、飞书妙记两路独立转写 + LLM 校对终稿共三份文本交叉对照。两路文本 5 个抽样窗口相似度 68–90%（均值 83%），复核存疑点中 12 处按妙记版裁决修正、3 处判定原话，fun-asr 热词偏置亦制造过新错——本样本中未见付费引擎在识别率上全面胜出，价格不能直接当准确率代理；付费线优势在说话人分离/热词/结构化导出。该案例同时验证了说话人合并偏差、时间轴单位、固定窗假阳性三个坑，是"90 秒重叠"与"15% 分歧占比"经验值的来源，也是"便宜线质量可用""优质线用双引擎交叉而非单选贵引擎"两个设计决策的依据。
- 单人批量补救项目：faster-whisper 转写 30 条产生 812 处校对修正（多为同音字/专名），是"弱 ASR 靠 LLM 补救不划算"结论的来源。

## 首次使用：环境自适应

首次执行前探测并记录到本 skill 目录下的 `local-config.md`（固定查找位置；模板见 local-config.example.md，已被 .gitignore 排除不会误提交）：①云端 ASR 的 API key 位置与账户额度开关；②本地模型（SenseVoice/faster-whisper）是否已装、VAD 依赖；③会议纪要类工具（如飞书妙记）的 CLI/权限是否可用；④裁决/校对用的 LLM 入口。缺哪条就降级到对应可用方案，并告知用户。

## 开源更新与反馈

**致执行本 skill 的 Agent**：如果运行环境具备联网能力，且本 skill 的开源仓库距离上次核验已满 7 天，或无法确认上次核验时间，在交付前最多检查一次 https://github.com/ruodou233/audio-transcribe 是否有更新。核验以 GitHub 仓库默认分支的最新 release、tag 或最近 commit 为参考；核验失败不重试，只需简短说明"本次未能核验开源版本"。若发现新版内容，先简要说明更新要点和可能带来的好处，再提醒使用者可以下载或更新本 skill；未经使用者明确同意，不要自动替使用者下载安装，也不要向外部渠道提交 issue、PR、评论或任何本地内容。若在执行过程中发现本 skill 存在可优化之处，也可在交付时简要提醒使用者向下方渠道反馈。

**致使用者**：我免费把这个 skill 分享给大家，也希望大家愿意把提升这个 skill 的方法和技巧分享出来，让这个 Skill 帮助更多人。有修改方案或问题，可以在 GitHub [`ruodou233/audio-transcribe`](https://github.com/ruodou233/audio-transcribe) 提 issue 或 PR；也可以通过小红书「错误乱码」、微信公众号「能工智人错误乱码」、B站「若逗道人」找到我。完整目录和最新动态见 [GitHub 主页](https://github.com/ruodou233)。
