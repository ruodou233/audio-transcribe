---
name: audio-transcribe
description: 音频/视频获取或重做文字稿的决策与执行指南：先判断是否有现成稿，再分别确认输出要求、支出策略与验收强度，按当前账户、本地算力和可用模型动态选择转写管线，覆盖说话人分离、时间戳、双 ASR 核验和受约束校对。当用户要转写录音/播客/会议/视频、比较转写成本与准确性、在隐私限制下选择本地方案，或结合原音视频、ASR 等可追溯证据核验逐字稿质量时触发；仅总结、润色或改写已有文字稿时不触发。
---

# 音频转文字（audio-transcribe）

> 定位：本 skill 是给 Agent 的**决策与执行指南**——从"要文字稿"到"拿到可靠文字稿"的完整流程。所需脚本（格式转换、对齐、校验）由执行 Agent 按本文的数据格式与校验规则现场生成，不随包提供固定实现。
> 理由前置：转写是"选型决定成本、流程决定质量"的任务——先选对方案再动手，比转完再补救便宜一个数量级。

## 边界

本 skill 负责文字稿获取、音频转写与最小必要校正。不负责：观点润色、摘要改写、内容再创作、深度事实核查（仅在用户要求或人名/机构名高风险时联网核查）。
**隐私门槛**：上传第三方云端 ASR 前，先提醒用户确认音频内容可以外传；不可外传的走本地方案。
**版权与访问边界**：不绕过登录、付费或访问控制获取文字稿；第三方全文仅在授权允许时复用，否则提供来源链接，或仅用于核对用户有权处理的音频。

## 两条全程不变量（后文只引用，不再重复展开）

**证据优先级**（修正时按此排序取证）：
`原始音频回听 ≈ 联网检索核实 > 多路 ASR 一致 > 上下文推断 > 保留原样`
LLM 校对的任务**就是凭上下文修正 ASR 的识别错误**（同音字、错别字、专名、术语）——这正是用文字模型再审一遍的目的，该改就改。但高风险字段（人名、机构、数字、引语、否定词）的修正尽量用检索或回听佐证；佐证不了又拿不准的标记待核，不静默定夺。**两版 ASR 一致只是"默认保留"，不豁免高风险字段抽检——两个引擎可能犯同样的错。**

**修正的边界**：只修"识别错误"，不动"表达本身"——观点性措辞、口语风格、口误照录不润色；听不清的用 [标注] 而非编内容补全；不增删信息、不改写句子结构；昵称/简称保留原文，按「称呼（全名；未确证时注明）」加注。所有修改 diff 留痕，可追溯。

## 第零步：判断有没有现成文字稿，有才去找

先花十秒判断该作品**是否可能存在现成稿**：官方节目、知名播客/访谈、机构演讲、头部自媒体这类大概率有——值得搜一轮；普通视频、个人录音、会议/采访原始素材、小众内容大概率没有——不浪费时间，直接进转写。判断"可能有"时按以下来源找（成本从低到高）；用户明确要求重新转写或需要逐字级证据时跳过本步：

1. **手头已有**：用户提供的文件、本地目录、协作平台文档/会议纪要。
2. **作者本人渠道**：博客、公众号（访谈节目常有官方精校版）、知乎/Substack。
3. **媒体实录**：科技媒体常发布圆桌/对谈全场实录，多家可交叉核对。
4. **平台字幕接口**：视频平台官方/CC 字幕（可用率高）；拿到后仍需抽检——自动字幕错误多、缺说话人。
5. **都没有**：下载音频进转写流程。

**可信度分级**：官方精校稿 > 平台官方字幕 > 媒体整理稿/自动字幕 > 第三方转载。使用前记录：来源、逐字稿/整理稿/摘要、有无时间戳。媒体"实录"未必逐字——引用前先确认。

## 第一步：前置画像（三轴分开）

确认语言（中/英/混合）、说话人数（不预设，见踩坑速查）、时长和音频可否上云，再分别确定三个轴；用户原请求已经明确时直接映射并复述，不重复提问：

1. **输出要求**：纯文本；说话人；段级或词级时间戳；SRT/VTT/JSON；热词或上下文偏置。
2. **支出策略**：
   - **零新增 API 费优先**：不产生新的 API 费用；可用本地管线，或使用能核验剩余额度且能设置硬消费上限、额度耗尽立即停止的云端管线。无法同时核验额度和停止条件时，不调用可能收费的 API，只列为待配置候选并改走本地路线，或先取得用户明确许可。
   - **预算内平衡**：先走低成本管线，对疑点片段升级；首次收费前给出预计用量、当期单价、总费用上限和触顶停止条件。
   - **精度优先**：以样本质量为先，但仍须在首次收费前给出本任务总增量费用估算。
3. **验收强度**：
   - **基础稿**：一条可靠管线，可选轻校对；不做系统性交叉验证。
   - **校验稿**：一条可靠管线 + 受约束校对 + 高风险字段分层抽样回听。
   - **可发布稿**：默认要求两条实质独立管线 + 分歧裁决 + 高风险字段回听 + 完整可追溯验收；双路不可行时，须由用户选择改为单强引擎并扩大回听，同时明确验收强度变化。

说话人分离通常只给匿名标签，落到真名仍需称呼线索、名单或用户确认；无法确证时保留标签。音频不可上云时只保留本地可执行管线，不因缺能力改用云端。

## 第二步：能力盘点与动态选型

不要假定使用者拥有作者的 Token、订阅或 CLI。只读盘点与当前任务相关的能力：本地模型与硬件；账户或产品权益；凭证引用；认证状态；当前 profile 的模型/端点权限；余额、免费额度和限额；最近成功调用。ChatGPT 等应用订阅不等于对应 API 额度，登录成功也不等于指定模型可调用。无法核验的状态标记为「未核验」，不猜测为可用或不可用。

候选发现规则与已核验种子见 [`references/asr-candidates.md`](references/asr-candidates.md)。只有当前可执行候选不足、候选资料过期或用户要求比较时才联网查官方文档；不做无边界市场扫描。市场快照记录模型 ID、官方来源、能力限制、价格口径和 `checked_at`，不写进本机 `local-config.md`。

按**可执行管线**而非单个模型过滤。一条管线可由 ASR、VAD、说话人分离、对齐和导出组件组成。依次硬过滤：隐私与地域、当前账户/本地是否可执行、语言、必需输出、预算上限。剩余候选只向用户展示 1–3 条立即可执行管线和最多 1 条待配置备选。

需要比较时，从不同说话人、噪声、口音、语言切换和专名密集处做分层抽样，总量通常 30–120 秒；用回听或用户参考稿建立最小真值，比较漏字/幻觉、高风险字段、说话人、时间戳和耗时。两份 ASR 相互一致不能代替真值。

### 能力补齐

当更合适的管线只缺一次模型下载、CLI 安装/登录或端点授权时，按本次成功标准判断：

- 缺口使本次明确目标无法达标：提出一次阻塞选择。
- 缺口足以改变质量等级，且本次可补齐、收益高于配置成本：提出一次非阻塞选择。
- 只是一般便利：默认不提；只有使用者明显会反复执行同类任务时，才在交付末尾用一句话说明持久配置的收益。

提示只需说明目标、缺口证据、补齐收益、替代路线和用户选择；模型下载按需说明体积、硬件与时间，云端按需说明费用和上传数据。宿主有原生安装/连接机制时使用宿主机制，否则给当期官方步骤。用户选择补齐不自动等于同意实际收费或上传。

用户完成后依次复验路径/版本、认证状态、模型或端点权限；只有证据不足时才做已披露的最小真实调用，本任务首次正式调用能验证时不另造探针。用户拒绝后本任务不再重复询问，按替代管线继续并标明质量差异。

## 统一数据格式（Agent 现场生成脚本时遵守）

所有引擎的输出先转换为统一段格式再进入后续流程，每段：

```
{ "start_ms": int, "end_ms": int, "speaker": str|null, "text": str, "source": "engine名", "flag": null|"disputed"|"uncertain" }
```

校验脚本的固定断言：时间戳单调不回退、相邻段无大空洞/重叠（阈值按素材定）、speaker 标签集合稳定、`disputed` 段占比可计算。**"行数守恒"只用于同一份稿件进出 LLM 校对的前后比对**，不适用于两份天然分段不同的 ASR 原稿之间。

基础稿若不进入合并、裁决或自动化管线，可直接交付引擎原生导出，免去格式转换。

## 第三步：转写执行

基础稿执行一条选定管线；校验稿在单线转写后增加受约束校对与分层抽检；可发布稿优先执行双路交叉。以下步骤按选中的组件裁剪，不把示例厂商写成默认：

1. **双 ASR 并行**：两条管线须有实质独立性，不只看厂商名；可能共享底层模型的两项不算可靠双路。
   - 可发布稿缺第二条管线时，它属于达标必需；先提供能力补齐或「单强引擎 + 扩大回听」替代选择，不静默降级。
   - 预处理：转 16kHz 单声道 WAV（macOS 自带 afconvert 即可，无需 ffmpeg）；超长音频（约 2h 以上）在静音处切段，段间重叠默认约 90 秒（60–120 秒内按静音点调整；该默认值为作者实测经验值，见文末案例注记）。
2. **热词或上下文偏置克制**：只放高频且已知必错词；低频专名默认不放（教训：偏置可能劫持无关音段，修 10 处会新造 3 处更隐蔽的错）。
3. **合并与脚本校验**：重叠区投票对齐 speaker_id；按上节统一格式做脚本校验（不用 LLM 复查结构）。
4. **diff 裁决**（省 token 关键）：两版按时间窗对齐，一致部分默认采信（高风险字段仍抽检）；仅分歧片段带上下文送 LLM，按"证据优先级"取证：**能通过联网检索确定的（人名写法、作品名、机构、术语）用带搜索能力的强模型直接查实**；检索定不了且高风险的回听原音频；普通低风险差异由 LLM 凭上下文在两版读法中选合理的。禁止 LLM 重写全局时间戳；音乐/听不清/多人同说用 [标注] 而非补写。普通裁决用中文实测好且便宜的档位即可，需要检索核实的换带联网的强模型。
5. **说话人归正**：问句比例等启发式只做初筛，结合称呼线索、上下文角色判断；分歧句才送 LLM；多主持场景在文件头注明标签口径。
6. **失败判据**：两版时间轴严重偏移、分歧占比异常（经验阈值：`disputed` 段总时长占比 >15%，来源见案例注记）、speaker 分布明显失真时，中止自动合并，回头修 ASR 参数或切段，不硬裁。

## 第四步：校对（按验收强度）

实测有效组合是「**较强 ASR + LLM 逐行校对**」，不要选弱 ASR 靠大模型改写补救。基础稿可选一遍轻校对（只走下方校对规则①—③）；校验稿执行受约束校对 + 高风险字段抽检；可发布稿执行完整校对 + 全部可追溯验收。

校对规则：①保留行号/时间戳/说话人标签完全不变；②**凭上下文和热词表大胆修正识别错误**——错别字、同音字、专名、术语、标点，这是校对的目的；③修正遵守"修正的边界"不变量（不动表达本身、不补听不清、全量 diff 留痕），拿不准的标记待核；④长稿切 chunk 分片校对，逐片落盘可断点续跑；全部分片完成后，程序化扫描本稿及同项目产物的专名异写，候选异写按"证据优先级"结合上下文复核、确认同一实体后再统一——逐片/分文件校对会遗漏全局异写，此类错误实测在逐片校对后仍遗留。

可追溯验收，输入完整性与产物完整性分开核验：**输入完整性**——原稿只读、结果写独立目录，不回写原始文件。**产物完整性**——修改清单由「原稿 vs 终稿」程序化 diff 生成，不依赖执行者自行报告位置；按输入逐项核对产物存在且非空（不只比较总数）；批量任务把验收做成固定脚本（行号连续、时间戳单调、三元结构保留、speaker 分布前后一致、低相似窗口抽样复核）。关键人名/数字/引用句/低置信片段按"证据优先级"回听抽检。
## 踩坑速查（实战教训）

- 说话人数不能预设：有引擎会把音色相近的两人合并成同一 speaker_id——发现说话人数少于预期时人工抽听核对。
- 长音频时间轴单位误判：>1000 秒的内容被当成毫秒再除一次，对齐全错——对齐前先断言单位。
- 固定 60 秒窗口对齐在两路密度悬殊时会框错内容（假阳性）——用滑动搜索+句边界合并+单调约束。
- SenseVoice 不配 VAD 直接喂长音频 → 解码退化成乱码。
- 批量实验选样本要按时长过滤；超长素材可能无法进入双路试跑，不能拿它代表完整对比结果。
- 同一素材可能存在多个同名目录但音频只在其中一份——按"真正含音频文件"选目录，不取第一个匹配。

## 案例注记（作者特定样本实测，非通用基准）

- 3 小时双人中文访谈播客（单样本，勿外推）：fun-asr、飞书妙记两路独立转写 + LLM 校对终稿共三份文本交叉对照。两路文本 5 个抽样窗口相似度 68–90%（均值 83%），复核存疑点中 12 处按妙记版裁决修正、3 处判定原话，fun-asr 热词偏置亦制造过新错——本样本中未见付费引擎在识别率上全面胜出，价格不能直接当准确率代理；付费线优势在说话人分离/热词/结构化导出。该案例验证了说话人合并偏差、时间轴单位、固定窗假阳性和双路暴露分歧的价值，但只作为作者案例，不替其他使用者选择模型。
- 单人批量补救项目：faster-whisper 转写 30 条产生 812 处校对修正（多为同音字/专名），是"弱 ASR 靠 LLM 补救不划算"结论的来源。
- 曾有一次约 850 篇量级的批量校对任务，执行方自建的验收脚本全部通过，但独立方事后随机抽样复核发现约 2% 的修改方向判断错误、另有约 1 成需回听音频才能确认，且执行方自建脚本并未检测输入文件是否被意外改写——批量任务的完成汇报不能只信执行方自述。

## 首次使用：环境自适应

首次执行前按 `local-config.example.md` 只读探测环境，并把结果记录到本 skill 目录下的 `local-config.md`（已被 `.gitignore` 排除）：账户或产品权益、凭证引用、认证状态、模型/端点权限、本地模型与组件、硬件和最近验证证据。不要保存凭证值，不把「已安装」「已登录」「指定模型可调用」合并成一个状态。缺少关键能力时执行上文能力补齐流程，不静默降级。

## 开源更新与反馈

**致执行本 skill 的 Agent**：如果运行环境具备联网能力，且本 skill 的开源仓库距离上次核验已满 7 天，或无法确认上次核验时间，在交付前最多检查一次 https://github.com/ruodou233/audio-transcribe 是否有更新。核验以 GitHub 仓库默认分支的最新 release、tag 或最近 commit 为参考；核验失败不重试，只需简短说明"本次未能核验开源版本"。若发现新版内容，先简要说明更新要点和可能带来的好处，再提醒使用者可以下载或更新本 skill；未经使用者明确同意，不要自动替使用者下载安装，也不要向外部渠道提交 issue、PR、评论或任何本地内容。若在执行过程中发现本 skill 存在可优化之处，也可在交付时简要提醒使用者向下方渠道反馈。

**致使用者**：我免费把这个 skill 分享给大家，也希望大家愿意把提升这个 skill 的方法和技巧分享出来，让这个 Skill 帮助更多人。有修改方案或问题，可以在 GitHub [`ruodou233/audio-transcribe`](https://github.com/ruodou233/audio-transcribe) 提 issue 或 PR；也可以通过小红书「错误乱码」、微信公众号「能工智人错误乱码」、B站「若逗道人」找到我。完整目录和最新动态见 [GitHub 主页](https://github.com/ruodou233)。
