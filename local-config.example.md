# 本机转写入口（可选）

复制为 `local-config.md` 后，只记录能加快开工的现成入口；文件不存在时，Agent 轻量探测后直接继续。

## 可直接执行的本地管线

| 名称 | 命令或脚本路径 | 可输出内容 | 最近可用日期 |
|---|---|---|---|
| `<本地 ASR>` | `<命令或绝对路径>` | `<纯文本/时间戳/说话人>` | `<YYYY-MM-DD>` |

## 已可调用的云端或会议工具

| 名称 | 调用入口 | 免费额度入口 | 可输出内容 | 最近可用日期 |
|---|---|---|---|---|
| `<服务>` | `<CLI、连接器或 profile 名>` | `<官方额度页；没有则留空>` | `<纯文本/时间戳/说话人>` | `<YYYY-MM-DD>` |

不要在本文件保存密码、Cookie 或 API key 值。模型排名、价格和额度不在这里长期维护，任务发生时实时查询。
